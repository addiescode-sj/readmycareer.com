// When using parallel routes, page.tsx is passed as the children slot.
// All content is rendered in the @upload, @goal, @plan, and @chat slots.
export default function RootPage() {
  return null;
}
