{
  "_comment": "Single source of truth for per-1M-token USD list prices, read by both the TypeScript agent layer (agents/lib/models.ts) and the TypeScript eval harness (eval/src/lib/pricing.ts). Update prices here only.",
  "gemini": { "input": 0.075, "output": 0.3 },
  "openai": { "input": 0.15, "output": 0.6 }
}
