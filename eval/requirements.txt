ragas==0.1.21
datasets>=2.20.0
# Pin to the 1.x line: ragas 0.1.21 pulls langchain-community (langchain<0.3), while
# langchain-google-genai 2.x requires langchain-core>=0.3 — installing them together is
# unresolvable. The 1.x line stays on the langchain<0.3 stack ragas 0.1.21 needs.
langchain-google-genai>=1.0.0,<2.0.0
# New unified SDK (>=5.1) — required for integrated-inference search (index.search with
# server-side embedding). The old `pinecone-client` lacks the integrated search API.
pinecone>=5.1.0
python-dotenv>=1.0.0
pandas>=2.0.0
# Additional dependencies for agent_harness.py
jsonschema>=4.0.0
httpx>=0.27.0
tabulate>=0.9.0
google-generativeai>=0.8.0